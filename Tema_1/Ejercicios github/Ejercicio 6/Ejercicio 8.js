let edad = 18;
if (edad === 18) {
  console.log("Tienes 18 años");
}
/**En JavaScript, = sirve para asignar valores, mientras que == o === se usan para comparar valores. 
 * Usar = en un if no compara, sino que asigna, y la condición siempre será verdadera. 
 * Por eso hay que usar === para comparar y asegurarse de que la condición solo se cumpla 
 * cuando edad sea realmente 18. */