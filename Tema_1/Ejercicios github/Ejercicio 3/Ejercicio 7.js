// Inicializamos el contador
let contador = 1;
do {
    console.log(contador); // Mostramos el número actual
    contador++;            // Incrementamos el contador
} while (contador <= 5);   // Condición para seguir el bucle