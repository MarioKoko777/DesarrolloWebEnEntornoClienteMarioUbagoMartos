// Inicializamos variables
let suma = 0;
let numero = 1;
// Bucle while: se ejecuta mientras la suma sea menor a 20
while (suma < 20) {
    suma += numero; // Sumamos el número actual
    numero++;       // Pasamos al siguiente número
}
// Mostramos el resultado final
console.log("La suma final es:", suma);