// Guardamos el número en una variable
let numero = 5; // Puedes cambiar este valor por cualquier número
// Usamos un bucle for para recorrer del 1 al 10
for (let i = 1; i <= 10; i++) {
    // Mostramos el resultado de la multiplicación
    console.log(`${numero} x ${i} = ${numero * i}`);
}